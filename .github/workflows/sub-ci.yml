name: CI

on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        version: [10, 12, 14]
    steps:
      - name: Run tests
        run: |
          echo "Hello ${{ matrix.version }}"
